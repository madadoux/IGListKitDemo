# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CollectionViewDemos' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'IGListKit'

  # Pods for CollectionViewDemos

  target 'CollectionViewDemosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CollectionViewDemosUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
